"use client";

import { useEffect, useRef } from "react";

export function useScrollAnimation(options = {}) {
  const ref = useRef(null);
  // Scroll animation logic will be added later
  return { ref };
}
